# All optional — works with zero config via `docker run`

# Override coordinator URL (default: convertsmedia production worker)
# COORDINATOR_URL=https://convertsmedia-api.keith-275.workers.dev

# Node identification (defaults to hostname)
# NODE_NAME=my-home-node

# Node type: residential (default), cloud, fly
# NODE_TYPE=residential

# Region hint for the coordinator
# REGION=us-west

# Bandwidth limit in GB (0 = unlimited, 100 = Fly.io free tier)
# BANDWIDTH_LIMIT_GB=0
